import { CustgenderPipe } from './custgender.pipe';

describe('CustgenderPipe', () => {
  it('create an instance', () => {
    const pipe = new CustgenderPipe();
    expect(pipe).toBeTruthy();
  });
});
